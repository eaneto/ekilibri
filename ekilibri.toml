servers = [
    "127.0.0.1:8081",
    "127.0.0.1:8082",
    "127.0.0.1:8083"
]
strategy = "LeastConnections"
# Maximum number of failed requests to stop balancing to.
max_fails = 5
# Timeout to consider a request as failed in milliseconds.
fail_timeout = 1000